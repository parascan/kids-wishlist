---
phase: 01-odds-math-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/package.json
  - backend/tsconfig.json
  - backend/vitest.config.ts
  - backend/tests/math/odds.test.ts
  - backend/tests/math/vig.test.ts
  - backend/tests/math/consensus.test.ts
  - backend/tests/math/teams.test.ts
  - backend/src/data/team-map.json
autonomous: true
requirements:
  - NORM-01
  - NORM-02
  - NORM-03
  - NORM-04

must_haves:
  truths:
    - "Running `npm --prefix backend test` executes Vitest and all tests FAIL (RED phase of TDD)"
    - "Test files contain exact test cases from research for all 4 requirements"
    - "team-map.json contains entries for all 68 March Madness teams with at least 3 variants each"
  artifacts:
    - path: "backend/package.json"
      provides: "Node project with vitest, typescript, @types/node devDependencies"
      contains: "vitest"
    - path: "backend/tsconfig.json"
      provides: "TypeScript config with strict mode, ESM, resolveJsonModule"
      contains: "resolveJsonModule"
    - path: "backend/vitest.config.ts"
      provides: "Vitest configuration"
      contains: "defineConfig"
    - path: "backend/tests/math/odds.test.ts"
      provides: "NORM-01 test cases for americanToImplied"
      contains: "americanToImplied"
    - path: "backend/tests/math/vig.test.ts"
      provides: "NORM-02 test cases for removeVig"
      contains: "removeVig"
    - path: "backend/tests/math/consensus.test.ts"
      provides: "NORM-03 test cases for simpleConsensus"
      contains: "simpleConsensus"
    - path: "backend/tests/math/teams.test.ts"
      provides: "NORM-04 test cases for resolveTeam"
      contains: "resolveTeam"
    - path: "backend/src/data/team-map.json"
      provides: "Static team name dictionary for 68 March Madness teams"
      min_lines: 200
  key_links:
    - from: "backend/tests/math/odds.test.ts"
      to: "backend/src/math/odds.ts"
      via: "import { americanToImplied, impliedToAmerican }"
      pattern: "import.*from.*math/odds"
    - from: "backend/tests/math/teams.test.ts"
      to: "backend/src/data/team-map.json"
      via: "resolveTeam reads from team-map.json"
      pattern: "resolveTeam"
---

<objective>
Scaffold the backend project and write all failing tests (TDD RED phase) for the odds math engine.

Purpose: Establish the test infrastructure and define the exact behavioral contracts for all 4 NORM requirements before writing any implementation. This ensures the math is driven by tests, not by implementation assumptions.

Output: A `backend/` directory with package.json, tsconfig, vitest config, 4 test files with concrete test cases, and a seeded team-map.json. All tests should fail because the source modules do not exist yet.
</objective>

<execution_context>
@/Users/chris/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-odds-math-engine/01-RESEARCH.md
@.planning/phases/01-odds-math-engine/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold backend project with TypeScript and Vitest</name>
  <files>backend/package.json, backend/tsconfig.json, backend/vitest.config.ts</files>
  <read_first>
    .planning/phases/01-odds-math-engine/01-RESEARCH.md
  </read_first>
  <action>
Create the `backend/` directory at the repo root (sibling to `wishlist-app/` and `chore-app/`).

**backend/package.json:**
```json
{
  "name": "march-madness-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.1.0",
    "@types/node": "^25.5.0",
    "tsx": "^4.21.0"
  }
}
```

**backend/tsconfig.json:**
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ES2022",
    "moduleResolution": "bundler",
    "strict": true,
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "declaration": true,
    "outDir": "dist",
    "rootDir": "src",
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true
  },
  "include": ["src/**/*.ts"],
  "exclude": ["node_modules", "dist", "tests"]
}
```

**backend/vitest.config.ts:**
```typescript
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    include: ['tests/**/*.test.ts'],
    globals: false,
  },
});
```

Then run `npm --prefix backend install` to generate node_modules and package-lock.json.
  </action>
  <verify>
    <automated>ls backend/package.json backend/tsconfig.json backend/vitest.config.ts && npm --prefix backend test 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - backend/package.json contains "vitest": "^4.1.0"
    - backend/package.json contains "typescript": "^5.9.3"
    - backend/package.json contains "type": "module"
    - backend/package.json contains "test": "vitest run"
    - backend/tsconfig.json contains "resolveJsonModule": true
    - backend/tsconfig.json contains "strict": true
    - backend/tsconfig.json contains "moduleResolution": "bundler"
    - backend/vitest.config.ts contains "defineConfig"
    - backend/vitest.config.ts contains "tests/**/*.test.ts"
    - backend/node_modules directory exists (npm install succeeded)
  </acceptance_criteria>
  <done>Backend project scaffolded with TypeScript, Vitest, and correct configuration. `npm --prefix backend test` runs without config errors (exits with "no tests found" or similar, not a config failure).</done>
</task>

<task type="auto">
  <name>Task 2: Write failing tests for NORM-01, NORM-02, NORM-03 (odds, vig, consensus)</name>
  <files>backend/tests/math/odds.test.ts, backend/tests/math/vig.test.ts, backend/tests/math/consensus.test.ts</files>
  <read_first>
    .planning/phases/01-odds-math-engine/01-RESEARCH.md
    backend/vitest.config.ts
  </read_first>
  <action>
Create test files that import from not-yet-existing source modules. Tests define the exact behavioral contract.

**backend/tests/math/odds.test.ts:**
```typescript
import { describe, it, expect } from 'vitest';
import { americanToImplied, impliedToAmerican } from '../../src/math/odds.js';

describe('americanToImplied', () => {
  it('converts positive odds (underdog): +150 -> 0.4', () => {
    expect(americanToImplied(150)).toBeCloseTo(0.4, 10);
  });

  it('converts negative odds (favorite): -200 -> 0.6667', () => {
    expect(americanToImplied(-200)).toBeCloseTo(0.6667, 4);
  });

  it('converts typical spread line: -110 -> 0.5238', () => {
    expect(americanToImplied(-110)).toBeCloseTo(0.5238, 4);
  });

  it('converts even money positive: +100 -> 0.5', () => {
    expect(americanToImplied(100)).toBeCloseTo(0.5, 10);
  });

  it('converts even money negative: -100 -> 0.5', () => {
    expect(americanToImplied(-100)).toBeCloseTo(0.5, 10);
  });

  it('converts big underdog: +300 -> 0.25', () => {
    expect(americanToImplied(300)).toBeCloseTo(0.25, 10);
  });

  it('throws RangeError for odds of 0', () => {
    expect(() => americanToImplied(0)).toThrow(RangeError);
  });

  it('handles heavy favorite: -10000 -> ~0.9901', () => {
    expect(americanToImplied(-10000)).toBeCloseTo(0.9901, 4);
  });

  it('handles heavy underdog: +10000 -> ~0.0099', () => {
    expect(americanToImplied(10000)).toBeCloseTo(0.0099, 4);
  });
});

describe('impliedToAmerican', () => {
  it('converts >= 50% probability to negative odds: 0.6667 -> ~-200', () => {
    expect(impliedToAmerican(0.6667)).toBeCloseTo(-200, 0);
  });

  it('converts < 50% probability to positive odds: 0.4 -> +150', () => {
    expect(impliedToAmerican(0.4)).toBeCloseTo(150, 0);
  });

  it('converts 50% to -100', () => {
    expect(impliedToAmerican(0.5)).toBeCloseTo(-100, 0);
  });

  it('round-trips: americanToImplied(impliedToAmerican(p)) ≈ p', () => {
    const p = 0.35;
    expect(americanToImplied(impliedToAmerican(p))).toBeCloseTo(p, 10);
  });
});
```

**backend/tests/math/vig.test.ts:**
```typescript
import { describe, it, expect } from 'vitest';
import { removeVig } from '../../src/math/vig.js';

describe('removeVig', () => {
  it('normalizes standard -110/-110 market to 0.5/0.5', () => {
    const result = removeVig({ impliedA: 0.52381, impliedB: 0.52381 });
    expect(result.fairA).toBeCloseTo(0.5, 4);
    expect(result.fairB).toBeCloseTo(0.5, 4);
    expect(result.overround).toBeCloseTo(1.04762, 4);
  });

  it('normalizes asymmetric market correctly', () => {
    const result = removeVig({ impliedA: 0.6667, impliedB: 0.4 });
    expect(result.fairA).toBeCloseTo(0.6250, 4);
    expect(result.fairB).toBeCloseTo(0.3750, 4);
    expect(result.overround).toBeCloseTo(1.0667, 4);
  });

  it('handles no-vig market (sum already 1.0)', () => {
    const result = removeVig({ impliedA: 0.5, impliedB: 0.5 });
    expect(result.fairA).toBeCloseTo(0.5, 10);
    expect(result.fairB).toBeCloseTo(0.5, 10);
    expect(result.overround).toBeCloseTo(1.0, 10);
  });

  it('always produces fairA + fairB = 1.0', () => {
    const testCases = [
      { impliedA: 0.52381, impliedB: 0.52381 },
      { impliedA: 0.6667, impliedB: 0.4 },
      { impliedA: 0.8, impliedB: 0.3 },
      { impliedA: 0.55, impliedB: 0.55 },
    ];
    for (const market of testCases) {
      const result = removeVig(market);
      expect(result.fairA + result.fairB).toBeCloseTo(1.0, 10);
    }
  });
});
```

**backend/tests/math/consensus.test.ts:**
```typescript
import { describe, it, expect } from 'vitest';
import { simpleConsensus } from '../../src/math/consensus.js';

describe('simpleConsensus', () => {
  it('averages three sources: [0.62, 0.60, 0.61] -> 0.61', () => {
    expect(simpleConsensus([0.62, 0.60, 0.61])).toBeCloseTo(0.61, 10);
  });

  it('averages two sources (Polymarket absent): [0.62, 0.60] -> 0.61', () => {
    expect(simpleConsensus([0.62, 0.60])).toBeCloseTo(0.61, 10);
  });

  it('returns single source as-is: [0.60] -> 0.60', () => {
    expect(simpleConsensus([0.60])).toBeCloseTo(0.60, 10);
  });

  it('throws Error on empty array', () => {
    expect(() => simpleConsensus([])).toThrow('Cannot compute consensus');
  });

  it('handles boundary probabilities: [0.0, 1.0] -> 0.5', () => {
    expect(simpleConsensus([0.0, 1.0])).toBeCloseTo(0.5, 10);
  });
});
```

All three test files must import from source modules that do NOT exist yet. Running `npm --prefix backend test` should show all tests failing with import/module-not-found errors.
  </action>
  <verify>
    <automated>npm --prefix backend test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - backend/tests/math/odds.test.ts contains "americanToImplied(150)" and "toBeCloseTo(0.4"
    - backend/tests/math/odds.test.ts contains "americanToImplied(0)).toThrow(RangeError)"
    - backend/tests/math/odds.test.ts contains "impliedToAmerican"
    - backend/tests/math/vig.test.ts contains "removeVig" and "impliedA: 0.52381"
    - backend/tests/math/vig.test.ts contains "fairA + result.fairB).toBeCloseTo(1.0"
    - backend/tests/math/consensus.test.ts contains "simpleConsensus" and "Cannot compute consensus"
    - backend/tests/math/consensus.test.ts contains "[0.62, 0.60, 0.61]"
    - npm --prefix backend test exits with non-zero (tests fail because source modules do not exist)
  </acceptance_criteria>
  <done>Three test files exist covering NORM-01 (9 americanToImplied cases + 4 impliedToAmerican cases), NORM-02 (4 removeVig cases), NORM-03 (5 simpleConsensus cases). All tests fail (RED) because source modules are not yet created.</done>
</task>

<task type="auto">
  <name>Task 3: Write failing tests for NORM-04 and seed team-map.json</name>
  <files>backend/tests/math/teams.test.ts, backend/src/data/team-map.json</files>
  <read_first>
    .planning/phases/01-odds-math-engine/01-RESEARCH.md
  </read_first>
  <action>
**backend/tests/math/teams.test.ts:**
```typescript
import { describe, it, expect } from 'vitest';
import { resolveTeam } from '../../src/math/teams.js';

describe('resolveTeam', () => {
  it('resolves short name: "Duke" -> "duke-blue-devils"', () => {
    expect(resolveTeam('Duke')).toBe('duke-blue-devils');
  });

  it('resolves full name: "Duke Blue Devils" -> "duke-blue-devils"', () => {
    expect(resolveTeam('Duke Blue Devils')).toBe('duke-blue-devils');
  });

  it('resolves all-caps: "DUKE" -> "duke-blue-devils"', () => {
    expect(resolveTeam('DUKE')).toBe('duke-blue-devils');
  });

  it('resolves common abbreviation: "Kansas" -> "kansas-jayhawks"', () => {
    expect(resolveTeam('Kansas')).toBe('kansas-jayhawks');
  });

  it('resolves full name: "Kansas Jayhawks" -> "kansas-jayhawks"', () => {
    expect(resolveTeam('Kansas Jayhawks')).toBe('kansas-jayhawks');
  });

  it('resolves nickname form: "UConn" -> "connecticut-huskies"', () => {
    expect(resolveTeam('UConn')).toBe('connecticut-huskies');
  });

  it('resolves state name: "Connecticut" -> "connecticut-huskies"', () => {
    expect(resolveTeam('Connecticut')).toBe('connecticut-huskies');
  });

  it('returns null for unknown team', () => {
    expect(resolveTeam('UnknownTeamXYZ')).toBeNull();
  });

  it('does not throw on unknown team', () => {
    expect(() => resolveTeam('UnknownTeamXYZ')).not.toThrow();
  });
});
```

**backend/src/data/team-map.json:**
Create a JSON object mapping team name variants to canonical IDs. Include all 68 teams in the 2026 March Madness bracket. For each team, include at minimum:
- Full official name (e.g., "Duke Blue Devils")
- Short/common name (e.g., "Duke")
- ALL-CAPS variant (e.g., "DUKE")
- Common abbreviation or nickname if different (e.g., "UConn" for Connecticut)

The canonical ID format is a lowercase slug: `{school}-{mascot}` (e.g., `duke-blue-devils`, `kansas-jayhawks`, `connecticut-huskies`).

Use the standard 68-team March Madness field. The 2026 bracket teams should be seeded from the known major conference teams and typical tournament participants. Include at minimum these teams (and all 68):

Top seeds and commonly known teams (partial list for reference):
- Duke Blue Devils, Kansas Jayhawks, UConn/Connecticut Huskies, Houston Cougars
- Purdue Boilermakers, North Carolina Tar Heels, Tennessee Volunteers, Auburn Tigers
- Iowa State Cyclones, Marquette Golden Eagles, Creighton Bluejays, Baylor Bears
- Arizona Wildcats, Kentucky Wildcats, Gonzaga Bulldogs, Alabama Crimson Tide
- Texas Longhorns, UCLA Bruins, Michigan State Spartans, Illinois Fighting Illini
- Clemson Tigers, San Diego State Aztecs, BYU Cougars, Wisconsin Badgers
- Florida Gators, St. Mary's Gaels, Oregon Ducks, Colorado State Rams
- Northwestern Wildcats, Dayton Flyers, Nevada Wolf Pack, New Mexico Lobos
- James Madison Dukes, Oakland Golden Grizzlies, Yale Bulldogs, Morehead State Eagles
- And remaining teams to reach 68 total

Each entry in the JSON is `"variant": "canonical-id"`. Multiple variants point to the same canonical ID.
  </action>
  <verify>
    <automated>npm --prefix backend test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - backend/tests/math/teams.test.ts contains "resolveTeam('Duke')).toBe('duke-blue-devils')"
    - backend/tests/math/teams.test.ts contains "resolveTeam('UConn')).toBe('connecticut-huskies')"
    - backend/tests/math/teams.test.ts contains "toBeNull()"
    - backend/src/data/team-map.json contains "duke-blue-devils"
    - backend/src/data/team-map.json contains "kansas-jayhawks"
    - backend/src/data/team-map.json contains "connecticut-huskies"
    - backend/src/data/team-map.json contains "DUKE"
    - backend/src/data/team-map.json contains "UConn"
    - backend/src/data/team-map.json has at least 200 lines (68 teams x 3+ variants)
    - npm --prefix backend test exits non-zero (teams.test.ts fails because src/math/teams.ts does not exist)
  </acceptance_criteria>
  <done>teams.test.ts has 9 test cases covering name resolution variants and null return for unknown teams. team-map.json is seeded with all 68 March Madness teams and at least 3 name variants each. Tests fail (RED) because src/math/teams.ts is not yet created.</done>
</task>

</tasks>

<verification>
After all 3 tasks complete:
1. `ls backend/package.json backend/tsconfig.json backend/vitest.config.ts` -- all exist
2. `ls backend/tests/math/odds.test.ts backend/tests/math/vig.test.ts backend/tests/math/consensus.test.ts backend/tests/math/teams.test.ts` -- all exist
3. `ls backend/src/data/team-map.json` -- exists with 68 teams
4. `npm --prefix backend test 2>&1` -- Vitest runs, all tests FAIL (RED phase complete)
5. No source files exist yet in `backend/src/math/` (odds.ts, vig.ts, consensus.ts, teams.ts do NOT exist)
</verification>

<success_criteria>
- Backend project is scaffolded with correct TypeScript + Vitest configuration
- All 4 test files contain the exact test cases from research
- team-map.json is populated with 68 teams and at least 200 name variant entries
- `npm --prefix backend test` runs Vitest successfully but all tests FAIL (TDD RED phase)
- Zero source implementation files exist -- only tests and data
</success_criteria>

<output>
After completion, create `.planning/phases/01-odds-math-engine/01-01-SUMMARY.md`
</output>
